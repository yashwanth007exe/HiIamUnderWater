#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main(){
    // multimap();
    // duplicate keys and sorted order
    // log(N)

    // unorderedmap();
    // unique keys but not sorted
    // O(1)                                           
}