#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main(){
    deque<int> dq;
    da.push_back(1);
    dq.emplace_back(2);

    dq.push_front(1);
    dq.emplace_front(0);

    dq.pop_back();
    dq.pop_front();

    dq.back(); // will return last element
    dq.front(); // will return first element

    // rest function same as vector
    // begin, end, rbeing, rend, clear, insert, size, swap
}